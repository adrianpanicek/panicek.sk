limit_req_zone $binary_remote_addr zone=portfolio_public_visits:1m rate=30r/m;

map $http_accept_encoding $portfolio_brotli_suffix {
    default "";
    "~*(^|,)\s*br\s*(,|$)" ".br";
}

map $portfolio_brotli_suffix $portfolio_content_encoding {
    default "";
    ".br" "br";
}
